import { Typography } from "@material-ui/core";

const FuelHistory = _ => {
  return (
    <Typography
      style={{
        margin: "10rem auto",
        textAlign: "center",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "3rem",
      }}
    >
      fuelHistory page{" "}
    </Typography>
  );
};
export default FuelHistory;
